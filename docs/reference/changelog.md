# Changelog

The canonical changelog lives at the repo root. It is generated from conventional commits via [`git-cliff`](https://git-cliff.org/).

{% include-markdown "../../CHANGELOG.md" rewrite-relative-urls=false comments=false %}
