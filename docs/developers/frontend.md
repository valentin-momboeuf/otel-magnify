# Frontend

> **Status:** Stub — to be expanded.

## Planned content

- React 18 + TypeScript + Vite.
- State: Zustand for UI state, TanStack Query for server state.
- YAML editing: CodeMirror 6 with the custom "Signal Deck" theme.
- Design system: warm gold accent, Plus Jakarta Sans + Fira Code, CSS variables in `styles/global.css`.
- Routing: `/inventory` is the primary agents page (not `/agents`).
- WebSocket client: reconnect strategy, token-query authentication.
- Playwright E2E — where tests live and how to run them.
